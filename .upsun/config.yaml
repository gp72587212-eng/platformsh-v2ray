applications:
  app:
    type: "php:8.3"

    web:
      commands:
        start: "php -S 0.0.0.0:$PORT router.php"

      locations:
        "/":
          passthru: true

routes:
  "https://{default}/":
    type: upstream
    upstream: "app:http"
